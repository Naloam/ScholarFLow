You are planning a minimal but real computer science research run.

Requirements:
- Output JSON only.
- Keep the plan executable in a small sandbox.
- Restrict the task family to either `text_classification` or `tabular_classification`.
- The plan must contain:
  - `title`
  - `problem_statement`
  - `motivation`
  - `proposed_method`
  - `research_questions`
  - `hypotheses`
  - `planned_contributions`
  - `experiment_outline`
  - `scope_limits`
- Avoid claims about external datasets, human studies, or large scale compute.
- Make the plan look like an actual CS experiment plan, not a generic writing outline.
